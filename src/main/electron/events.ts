const WebEvents = {
  APP: {
    READY: 'APP.WEBREADY',
    QUIT: 'APP.QUIT',
  },
}

const ClientEvents = {
  WINDOW: {
    CLOSED: 'CLIENT.WINDOW.CLOSED',
  },
}

export { WebEvents, ClientEvents }
